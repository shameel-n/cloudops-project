# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cloudops_demo
DB_USER=postgres
DB_PASSWORD=password

# For production/Kubernetes deployment, these will be:
# DB_HOST=postgres-service
# DB_PASSWORD=${POSTGRES_PASSWORD} (from secret) 