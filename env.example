# AWS RDS Database Configuration for Local Development
# Copy this file to .env and update with your RDS details

# Database Configuration (Get these from AWS RDS Console or CloudFormation outputs)
DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
DB_PORT=5432
DB_NAME=cloudops_demo
DB_USER=postgres
DB_PASSWORD=your-secure-password

# Backend Configuration
NODE_ENV=development
PORT=5000

# AWS Configuration (if needed for local development)
AWS_REGION=us-west-2
AWS_PROFILE=default

# Docker Compose Override
COMPOSE_FILE=docker-compose.yml 